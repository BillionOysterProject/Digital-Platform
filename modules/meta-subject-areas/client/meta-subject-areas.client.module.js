(function (app) {
  'use strict';

  app.registerModule('meta-subject-areas');
  app.registerModule('meta-subject-areas.services');
  //app.registerModule('meta-subject-areas.routes', ['ui.router', 'meta-subject-areas.services']);
})(ApplicationConfiguration);
