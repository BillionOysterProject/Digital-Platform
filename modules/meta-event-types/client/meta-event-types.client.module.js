(function (app) {
  'use strict';

  app.registerModule('meta-event-types');
  app.registerModule('meta-event-types.services');
  // app.registerModule('meta-event-types.routes', ['ui.router', 'meta-event-types.services']);
}(ApplicationConfiguration));
