(function (app) {
  'use strict';

  app.registerModule('sites');
  app.registerModule('sites.services');
  //app.registerModule('sites.routes', ['ui.router', 'sites.services']);
})(ApplicationConfiguration);